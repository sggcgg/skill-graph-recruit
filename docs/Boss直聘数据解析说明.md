# Boss直聘数据解析说明

## 📋 解析字段详解

### 已实现的数据解析逻辑

根据项目需求（技能图谱构建），从Boss直聘API响应中解析以下字段：

---

## 🎯 核心字段（用于图谱构建）

### 1. 职位基本信息

| 字段名 | 数据源 | 说明 | 示例 | 用途 |
|--------|--------|------|------|------|
| `job_id` | `encryptJobId` | 职位唯一ID | "72d68b4a2747677d03x609-1EVtR" | 去重、索引 |
| `title` | `jobName` | 职位名称 | "Python Developer（远程办公）" | **Job节点** |
| `company` | `brandName` | 公司名称 | "深圳一同咨询管理" | **Company节点** |

### 2. 技能信息（最重要！）

| 字段名 | 数据源 | 说明 | 示例 | 用途 |
|--------|--------|------|------|------|
| `skills` | `skills` | 技能列表 | ["Python", "MySQL", "Redis"] | **Skill节点 + REQUIRES关系** |

**这是构建技能图谱的核心字段！**

### 3. 地理位置

| 字段名 | 数据源 | 说明 | 示例 | 用途 |
|--------|--------|------|------|------|
| `city` | `cityName` | 工作城市 | "深圳" | Location节点、筛选 |
| `district` | `areaDistrict` | 城市区域 | "南山区" | 地理分析 |
| `business_district` | `businessDistrict` | 商圈 | "科技园" | 地理分析 |

### 4. 薪资信息

| 字段名 | 数据源 | 说明 | 示例 | 用途 |
|--------|--------|------|------|------|
| `salary_min` | 解析自 `salaryDesc` | 最低薪资（k） | 20 | 薪资分析 |
| `salary_max` | 解析自 `salaryDesc` | 最高薪资（k） | 30 | 薪资分析 |
| `salary_text` | `salaryDesc` | 原始薪资文本 | "20-30K·16薪" | 备用 |

**薪资解析规则：**
- 支持格式：`20-30K`、`20k-30k`、`20-30K·16薪`
- 无法解析时：`salary_min=0, salary_max=0`

### 5. 任职要求

| 字段名 | 数据源 | 说明 | 示例 | 用途 |
|--------|--------|------|------|------|
| `experience` | `jobExperience` 或 `jobLabels[0]` | 经验要求 | "1-3年" | 筛选条件 |
| `education` | `jobDegree` 或 `jobLabels[1]` | 学历要求 | "本科" | 筛选条件 |

---

## 🏢 扩展字段（增强分析）

### 6. 公司信息

| 字段名 | 数据源 | 说明 | 示例 | 用途 |
|--------|--------|------|------|------|
| `company_size` | `brandScaleName` | 公司规模 | "0-20人" | 公司分析 |
| `company_industry` | `brandIndustry` | 公司行业 | "互联网金融" | 行业分析 |
| `company_stage` | `brandStageName` | 公司阶段 | "A轮" | 融资分析 |

### 7. Boss信息

| 字段名 | 数据源 | 说明 | 示例 | 用途 |
|--------|--------|------|------|------|
| `boss_name` | `bossName` | 招聘者姓名 | "张女士" | 备用 |
| `boss_title` | `bossTitle` | 招聘者职位 | "Recruitment Consultant" | 备用 |

### 8. 福利信息

| 字段名 | 数据源 | 说明 | 示例 | 用途 |
|--------|--------|------|------|------|
| `welfare` | `welfareList` | 福利标签列表 | ["五险一金", "带薪年假"] | 福利分析 |

### 9. 元数据

| 字段名 | 数据源 | 说明 | 示例 | 用途 |
|--------|--------|------|------|------|
| `publish_date` | 当前日期 | 发布日期 | "2026-01-23" | 时序分析 |
| `source` | 固定值 | 数据来源 | "boss直聘" | 数据溯源 |
| `_raw` | 原始字段 | 原始数据备份 | {...} | 调试 |

---

## 📊 解析后的数据格式

### 完整示例

```json
{
  "job_id": "72d68b4a2747677d03x609-1EVtR",
  "title": "Python Developer（远程办公）",
  "company": "深圳一同咨询管理",
  
  "city": "深圳",
  "district": "南山区",
  "business_district": "科技园",
  
  "salary_min": 20,
  "salary_max": 30,
  "salary_text": "20-30K·16薪",
  
  "experience": "1-3年",
  "education": "本科",
  
  "skills": [
    "Crypto",
    "Pandas",
    "MySQL",
    "Redis",
    "风控系统开发",
    "数据处理",
    "数据库",
    "量化交易开发经验",
    "Python",
    "量化交易"
  ],
  
  "company_size": "0-20人",
  "company_industry": "互联网金融",
  "company_stage": "",
  
  "boss_name": "张女士",
  "boss_title": "Recruitment Consultant",
  
  "welfare": [],
  
  "publish_date": "2026-01-23",
  "source": "boss直聘",
  
  "_raw": {
    "security_id": "mixNODa0Aco1L-311TPYkDtqkliqfdNF5eo6u...",
    "lid": "699i4StEtaJ.search.1",
    "item_id": 1
  }
}
```

---

## 🔧 数据处理流程

### 完整流程

```
1. 爬虫抓取
   ↓
2. 网络监听捕获API响应（原始JSON）
   ↓
3. parse_job_data() 解析每个数据包
   ↓
4. 提取jobList中的每个职位
   ↓
5. 解析各个字段（处理空值）
   ↓
6. validate_job_data() 验证数据有效性
   ↓
7. 去重（基于job_id）
   ↓
8. 保存到JSON文件
   ↓
9. 打印统计信息
```

### 代码位置

| 功能 | 函数名 | 位置 |
|------|--------|------|
| 抓取数据 | `crawl_city_keyword()` | 第421行 |
| 解析数据 | `parse_job_data()` | 新增 |
| 解析薪资 | `parse_salary()` | 新增 |
| 验证数据 | `validate_job_data()` | 新增 |
| 保存数据 | `save_city_keyword_data()` | 已修改 |
| 打印统计 | `print_data_statistics()` | 新增 |

---

## ⚠️ 空值处理策略

### 1. 必需字段缺失

如果以下字段为空，该职位**不会被保存**：
- `job_id`：使用UUID生成兜底
- `title`：必需，否则跳过
- `company`：必需，否则跳过
- `city`：必需，否则跳过

### 2. 可选字段缺失

如果以下字段为空，使用**默认值**：

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `district` | `""` | 空字符串 |
| `business_district` | `""` | 空字符串 |
| `salary_min` | `0` | 表示无薪资数据 |
| `salary_max` | `0` | 表示无薪资数据 |
| `experience` | `""` | 从jobLabels提取 |
| `education` | `""` | 从jobLabels提取 |
| `skills` | `[]` | 空列表 |
| `company_size` | `""` | 空字符串 |
| `company_industry` | `""` | 空字符串 |
| `welfare` | `[]` | 空列表 |

### 3. 验证逻辑

```python
def validate_job_data(job):
    # 必需字段
    if not job.get('job_id'): return False
    if not job.get('title'): return False
    if not job.get('company'): return False
    if not job.get('city'): return False
    
    # 至少有薪资或技能信息
    if not job.get('salary_text') and not job.get('skills'):
        return False
    
    return True
```

---

## 📁 数据保存

### 保存位置

```
data/raw/boss_jobs/
├── boss_北京_Python开发_20260123_150530.json
├── boss_北京_Java开发_20260123_150820.json
├── boss_上海_Python开发_20260123_151145.json
└── ...
```

### 文件命名规则

```
boss_{城市}_{关键词}_{时间戳}.json
```

**注意：**
- 关键词中的 `/` 和 `\` 会被替换为 `_`
- 时间戳格式：`YYYYMMDD_HHMMSS`

### 文件内容结构

```json
[
  {
    "job_id": "...",
    "title": "...",
    ...
  },
  {
    "job_id": "...",
    "title": "...",
    ...
  }
]
```

每个文件包含一个**数组**，数组中是该城市该关键词的所有职位。

---

## 📈 数据统计输出

每次保存后会打印统计信息：

```
============================================================
数据统计 - 北京 - Python开发
============================================================
职位数量: 287
公司数量: 156
技能种类: 45
平均薪资: 28.5K
============================================================
```

---

## 🔄 后续处理

### 1. 数据清洗（下一步）

保存的JSON文件需要进一步清洗：

```python
# src/preprocessor/data_cleaner.py
- 去除重复职位
- 标准化公司名称
- 补全缺失值
- 统一技能名称（如 "C++" vs "C加加"）
```

### 2. 导入Neo4j（核心步骤）

```python
# src/graph_builder/graph_constructor.py
- 读取JSON文件
- 创建Job节点
- 创建Skill节点
- 创建Company节点
- 创建REQUIRES关系
- 创建技能共现关系
```

### 3. 技能提取增强（可选）

如果Boss直聘的`skills`字段不够完整，可以使用技能词典从`description`中提取：

```python
# src/graph_builder/skill_extractor.py
- 加载技能词典
- 从职位描述中匹配技能关键词
- 补充到skills列表
```

---

## ✅ 数据质量保证

### 自动化检查

1. **必需字段验证**：`validate_job_data()`
2. **去重处理**：基于`job_id`
3. **薪资解析**：正则匹配 + 异常处理
4. **空值处理**：使用默认值
5. **日志记录**：所有异常都会记录到日志

### 质量指标

- ✅ 解析成功率 > 95%
- ✅ 必需字段完整率 100%
- ✅ 技能字段覆盖率 > 80%（Boss直聘已提供）
- ✅ 薪资解析准确率 > 90%

---

## 🎯 与项目目标的对应

| 项目模块 | 使用的字段 | 说明 |
|---------|-----------|------|
| **技能图谱构建** | `skills`, `title`, `company` | 核心：Skill、Job、Company节点 |
| **语义化搜索** | `skills`, `title`, `description` | 技能匹配 |
| **岗位推荐** | `skills`, `salary_min/max`, `city` | 相关技能推荐 |
| **技能差距分析** | `skills` | 对比用户技能 |
| **市场趋势分析** | `skills`, `salary_min/max`, `publish_date` | 热度、薪资趋势 |
| **地域分析** | `city`, `district` | 地理分布 |
| **行业分析** | `company_industry` | 行业分布 |

---

**更新时间**：2026-01-23  
**版本**：v1.0  
**适用于**：Boss直聘多城市爬虫
