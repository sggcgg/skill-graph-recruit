说明

> 最后更新：2026-02-07

## 项目概览

skill-graph-recruit 是一个基于知识图谱和LLM的智能招聘分析系统，结合了传统图数据库（Neo4j）和现代AI技术（RAG、Agent）。

## 核心技术栈

- **图数据库**: Neo4j（存储技能图谱和关系）
- **向量数据库**: ChromaDB（存储岗位向量用于语义搜索）
- **大语言模型**: 通义千问（qwen-plus）
- **Embedding模型**: moka-ai/m3e-base（中文向量化）
- **框架**: FastAPI（API服务）、LangChain（Agent编排）
- **NLP**: Jieba分词、规则匹配、LLM增强

---

## 📁 目录结构

```
skill-graph-recruit/
├── config.yaml                    # 全局配置文件（API Key、数据库连接等）
├── requirements.txt              # Python依赖（基础）
├── requirements_ai.txt          # Python依赖（AI增强功能）
├── run_pipeline.py              # 数据处理主流程
├── run_api.py                   # API服务启动器
│
├── src/                         # 源代码目录
│   ├── crawler/                 # 爬虫模块
│   │   └── boss_listdata.py    # BOSS直聘爬虫
│   │
│   ├── data_processing/         # 数据处理模块
│   │   └── data_cleaner.py     # 数据清洗（去重、标准化）
│   │
│   ├── nlp/                     # NLP模块
│   │   ├── skill_extractor.py          # 规则技能抽取器（基础）
│   │   └── hybrid_skill_extractor.py   # 混合技能抽取器（规则+LLM）
│   │
│   ├── llm/                     # LLM模块
│   │   └── llm_client.py       # 通义千问客户端封装
│   │
│   ├── rag/                     # RAG模块
│   │   ├── vector_db.py        # ChromaDB向量数据库管理
│   │   └── rag_service.py      # RAG服务（检索+生成）
│   │
│   ├── agent/                   # Agent模块
│   │   ├── tools.py            # Agent工具定义
│   │   └── job_agent.py        # 职位推荐Agent
│   │
│   ├── graph_builder/          # 图数据库模块
│   │   ├── skill_dictionary.py    # 技能词典管理
│   │   ├── neo4j_manager.py       # Neo4j连接管理（简单）
│   │   └── neo4j_importer.py      # Neo4j数据导入（完整）
│   │
│   ├── api/                     # API模块
│   │   └── main.py             # FastAPI主应用
│   │
│   └── utils/                   # 工具模块
│       └── config.py           # 配置文件读取
│
├── scripts/                     # 脚本目录
│   ├── check_environment.py           # 环境检查
│   ├── download_m3e_model.py          # 下载Embedding模型
│   ├── fix_m3e_model.py              # 修复本地模型配置
│   ├── generate_crawl_keywords.py     # 生成爬虫关键词
│   ├── analyze_data_quality.py        # 数据质量分析
│   ├── generate_report.py            # 生成数据报告
│   ├── enhance_skills_with_llm.py    # LLM技能增强
│   ├── init_vector_db.py             # 初始化向量数据库
│   ├── reimport_neo4j.py             # 重新导入Neo4j
│   └── test_vpn_connection.py        # VPN代理测试
│
├── data/                        # 数据目录
│   ├── crawl_keywords.json     # 爬虫关键词
│   ├── cleaned/                # 清洗后的数据
│   ├── enhanced/               # LLM增强后的数据
│   └── skill_dict/             # 技能词典
│       ├── skill_taxonomy.json       # 技能分类体系
│       ├── suggested_skills.json     # 建议的技能
│       └── missing_skills.json       # 缺失的技能
│
├── docs/                        # 文档目录
│   ├── 项目代码结构说明.md          # 本文档
│   ├── LLM增强数据导入流程.md       # LLM增强流程
│   ├── VPN代理配置说明.md          # VPN配置指南
│   ├── AI应用方向转型规划.md       # 职业规划建议
│   └── 项目AI升级实施指南.md       # 技术实施指南
│
└── reports/                     # 报告目录
    ├── data_quality_report.html
    └── *.png                    # 可视化图表
```

---

## 📝 核心代码文件详解

### 1. 配置与环境

#### `config.yaml`
**作用**: 全局配置文件  
**内容**:
- LLM配置（API Key、模型选择、代理设置）
- Embedding配置（模型路径、设备选择）
- Neo4j连接配置
- ChromaDB配置
- RAG参数配置

**使用**: 所有模块都会读取此文件

#### `requirements_ai.txt`
**作用**: AI功能的Python依赖清单  
**包含**:
- openai (通义千问客户端)
- langchain (Agent框架)
- chromadb (向量数据库)
- sentence-transformers (Embedding)
- fastapi/uvicorn (API服务)
- py2neo (Neo4j驱动)

---

### 2. 数据获取与处理

#### `src/crawler/boss_listdata.py`
**作用**: BOSS直聘爬虫  
**功能**:
- 模拟浏览器访问
- 反爬虫检测与规避
- 多城市、多关键词爬取
- 数据持久化

**使用场景**: 获取原始招聘数据

#### `src/data_processing/data_cleaner.py`
**作用**: 数据清洗与标准化  
**功能**:
- 去重（基于job_id）
- 清理异常数据（空技能、异常薪资）
- 字段标准化（公司规模、学历、经验）
- 规则抽取技能

**输入**: 爬虫原始数据  
**输出**: `data/cleaned/boss_*_cleaned.json`

**使用**: 
```bash
python run_pipeline.py
```

---

### 3. NLP与技能抽取

#### `src/nlp/skill_extractor.py`
**作用**: 基于规则的技能抽取器（v1.0）  
**方法**:
- 直接匹配（技能词典精确匹配）
- 词级匹配（Jieba分词后匹配）
- 模式匹配（正则表达式）

**优点**: 快速、准确  
**缺点**: 只能识别词典中的技能

#### `src/nlp/hybrid_skill_extractor.py` ⭐
**作用**: 混合技能抽取器（v2.0，规则+LLM）  
**创新点**:
- 结合规则抽取和LLM理解
- 智能合并去重
- 置信度评分
- 识别隐含技能

**方法**:
```python
extractor = HybridSkillExtractor()
result = extractor.extract(job, use_llm=True)
# result包含: rule_skills, llm_skills, merged_skills, confidence
```

**使用场景**: LLM增强技能抽取

---

### 4. LLM集成

#### `src/llm/llm_client.py` ⭐
**作用**: 通义千问API客户端封装  
**功能**:
- 统一的对话接口
- 技能提取（extract_skills_from_jd）
- 技能差距分析（analyze_skill_gap）
- 岗位总结（summarize_jobs）
- 自动代理配置（支持VPN）

**核心方法**:
```python
client = LLMClient()
skills = client.extract_skills_from_jd(jd_text, known_skills)
analysis = client.analyze_skill_gap(user_skills, job_skills, target_position)
```

**配置**: 在 `config.yaml` 中设置API Key和代理

---

### 5. RAG（检索增强生成）

#### `src/rag/vector_db.py` ⭐
**作用**: ChromaDB向量数据库管理  
**功能**:
- 加载m3e-base Embedding模型
- 批量向量化岗位描述
- 语义搜索（similarity search）
- 支持过滤（城市、薪资、经验）

**核心方法**:
```python
db = VectorDB()
db.add_jobs(jobs)  # 批量添加
results = db.search(query, top_k=10, filters={'city': '北京'})
```

#### `src/rag/rag_service.py` ⭐
**作用**: RAG服务（检索+生成）  
**功能**:
- 语义搜索 + LLM总结
- 技能差距分析
- 岗位推荐
- 相似岗位查找

**核心方法**:
```python
rag = RAGService()
result = rag.search_and_summarize(query="Python后端岗位", top_k=10)
recommendations = rag.recommend_jobs(user_skills, target_position)
```

---

### 6. Agent智能对话

#### `src/agent/tools.py`
**作用**: 定义Agent可用的工具  
**工具列表**:
- 岗位语义搜索
- 技能差距分析
- 岗位推荐
- 技能图谱查询（Neo4j）

#### `src/agent/job_agent.py` ⭐
**作用**: LangChain职位推荐Agent  
**功能**:
- 多轮对话
- 工具调用（自动选择工具）
- 对话记忆
- 自然语言交互

**使用**:
```python
agent = JobRecommendAgent()
response = agent.chat("我会Python和Django，想找北京的后端岗位")
```

**测试**:
```bash
python src/agent/job_agent.py  # 进入交互式对话
```

---

### 7. 图数据库

#### `src/graph_builder/skill_dictionary.py`
**作用**: 技能词典管理  
**功能**:
- 加载技能分类体系（skill_taxonomy.json）
- 扁平化技能列表
- 按分类/级别/热度查询
- 技能搜索

#### `src/graph_builder/neo4j_manager.py`
**作用**: Neo4j连接管理（简单）  
**功能**:
- 连接测试
- 基础查询
- 统计信息

#### `src/graph_builder/neo4j_importer.py` ⭐
**作用**: Neo4j数据导入器（完整）  
**功能**:
- 创建索引
- 导入技能节点（从词典）
- 导入岗位和公司节点
- 创建关系（REQUIRES, POSTED_BY, RELATED_TO）
- 更新统计信息
- 批量处理（性能优化）

**节点类型**:
- Skill（技能）
- Job（岗位）
- Company（公司）

**关系类型**:
- (Job)-[REQUIRES]->(Skill)
- (Job)-[POSTED_BY]->(Company)
- (Skill)-[RELATED_TO]->(Skill)

---

### 8. API服务

#### `src/api/main.py` ⭐
**作用**: FastAPI REST API服务  
**端点列表**:

| 端点 | 方法 | 功能 |
|------|------|------|
| `/` | GET | API信息 |
| `/api/health` | GET | 健康检查 |
| `/api/skill/extract` | POST | 技能抽取 |
| `/api/rag/search` | POST | 语义搜索 |
| `/api/skill/gap-analysis` | POST | 技能差距分析 |
| `/api/job/recommend` | POST | 岗位推荐 |
| `/api/agent/chat` | POST | Agent对话 |
| `/api/stats` | GET | 系统统计 |

**启动**:
```bash
python run_api.py
# 访问: http://localhost:8000/docs
```

---

### 9. 实用脚本

#### `scripts/check_environment.py` ⭐
**作用**: 环境检查  
**检查项**:
- Python版本
- 依赖安装
- 配置文件存在
- Neo4j连接
- API Key配置

**使用**: 
```bash
python scripts/check_environment.py
```

#### `scripts/enhance_skills_with_llm.py` ⭐
**作用**: LLM技能增强  
**功能**:
- 测试模式（5条对比）
- 单城市增强
- 全量增强
- 仅规则抽取

**使用**:
```bash
python scripts/enhance_skills_with_llm.py
```

#### `scripts/init_vector_db.py` ⭐
**作用**: 初始化向量数据库  
**功能**:
- 加载清洗/增强数据
- 批量向量化
- 存储到ChromaDB
- 测试搜索

**使用**:
```bash
python scripts/init_vector_db.py
```

#### `scripts/reimport_neo4j.py` ⭐
**作用**: 重新导入Neo4j  
**功能**:
- 清空数据库
- 自动检测增强数据
- 重新导入
- 统计验证

**使用**:
```bash
python scripts/reimport_neo4j.py
```

#### `scripts/download_m3e_model.py`
**作用**: 下载m3e-base模型  
**使用**: 首次运行时下载Embedding模型

#### `scripts/fix_m3e_model.py`
**作用**: 修复本地模型配置  
**使用**: 当模型加载失败时运行

#### `scripts/test_vpn_connection.py` ⭐
**作用**: VPN代理测试  
**功能**:
- 测试国内外网络
- 测试通义千问API
- 提供诊断建议

**使用**:
```bash
python scripts/test_vpn_connection.py
```

#### `scripts/analyze_data_quality.py`
**作用**: 数据质量分析  
**输出**: 统计报告、异常检测

#### `scripts/generate_report.py`
**作用**: 生成可视化报告  
**输出**: HTML报告、图表

---

## 🔄 完整工作流程

### 流程图

```
1. 数据获取
   ├─ boss_listdata.py (爬虫)
   └─ 输出: 原始JSON

2. 数据清洗
   ├─ data_cleaner.py
   └─ 输出: data/cleaned/*.json

3. LLM增强（可选）⭐
   ├─ enhance_skills_with_llm.py
   ├─ 使用: hybrid_skill_extractor.py + llm_client.py
   └─ 输出: data/enhanced/*.json

4. 导入Neo4j
   ├─ reimport_neo4j.py
   ├─ 使用: neo4j_importer.py
   └─ 输出: Neo4j图数据库

5. 初始化向量库⭐
   ├─ init_vector_db.py
   ├─ 使用: vector_db.py
   └─ 输出: ChromaDB

6. 启动服务⭐
   ├─ run_api.py
   └─ FastAPI服务 (端口8000)

7. 使用Agent⭐
   └─ job_agent.py (交互式对话)
```

### 推荐执行顺序

```bash
# 第1步：环境检查
python scripts/check_environment.py

# 第2步：下载模型（首次运行）
python scripts/download_m3e_model.py

# 第3步：LLM增强（如果已有清洗数据）
python scripts/enhance_skills_with_llm.py
# 选择: 1 (测试模式) -> 验证效果
# 选择: 3 (全量增强) -> 正式处理

# 第4步：导入Neo4j
python scripts/reimport_neo4j.py
# 选择: 1 (增强数据，推荐)

# 第5步：初始化向量库
python scripts/init_vector_db.py

# 第6步：启动API服务
python run_api.py
# 访问: http://localhost:8000/docs

# 第7步：测试Agent
python src/agent/job_agent.py
```

---

## ⭐ 核心创新点（简历亮点）

### 1. 混合技能抽取（规则+LLM）
**文件**: `src/nlp/hybrid_skill_extractor.py`  
**创新**: 
- 规则保证精确性
- LLM识别隐含技能
- 智能合并去重
- 置信度评分

### 2. RAG智能检索
**文件**: `src/rag/rag_service.py`, `src/rag/vector_db.py`  
**创新**:
- 向量检索 + LLM生成
- 语义理解用户意图
- 个性化岗位推荐

### 3. LangChain Agent
**文件**: `src/agent/job_agent.py`, `src/agent/tools.py`  
**创新**:
- 多轮对话
- 自动工具选择
- 结合图谱和向量检索

### 4. 知识图谱构建
**文件**: `src/graph_builder/neo4j_importer.py`  
**创新**:
- 技能关系挖掘
- 多维度统计
- 支持30万数据规模

---

## 🗑️ 可删除的文件

以下文件可以安全删除（已有更好的替代）：

### 已经标记删除
- ❌ `src/crawler/demo1.py` - 测试文件
- ❌ `src/crawler/demo2.py` - 测试文件
- ❌ `src/crawler/boss_page.html` - 测试文件
- ❌ `quick_test.py` - 临时测试
- ❌ `test_paths.py` - 临时测试

### 建议保留但不常用
- `scripts/init_neo4j.py` - 功能被 `reimport_neo4j.py` 覆盖，但可保留作为参考
- `scripts/generate_crawl_keywords.py` - 关键词已生成，但可保留
- `src/utils/config.py` - 简单配置读取，被 `config.yaml` 直接读取替代

---

## 📦 依赖安装

```bash
# 安装所有依赖
pip install -r requirements_ai.txt

# 或使用清华镜像（国内推荐）
pip install -r requirements_ai.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 🔧 配置文件

### 必须配置项

在 `config.yaml` 中：

1. **通义千问API Key**（必须）:
   ```yaml
   llm:
     api_key: "sk-your-api-key-here"
   ```

2. **Neo4j密码**（必须）:
   ```yaml
   neo4j:
     password: "your-password"
   ```

3. **VPN代理**（如果使用）:
   ```yaml
   llm:
     proxy:
       enabled: true
       http_proxy: "http://127.0.0.1:7890"
   ```

---

## 📚 相关文档

- **LLM增强数据导入流程.md** - LLM使用详细说明
- **VPN代理配置说明.md** - 代理配置指南
- **AI应用方向转型规划.md** - 职业规划建议
- **项目AI升级实施指南.md** - 技术实施详解

---

## 🎯 快速定位

### 想修改爬虫？
→ `src/crawler/boss_listdata.py`

### 想优化技能抽取？
→ `src/nlp/hybrid_skill_extractor.py`

### 想调整LLM提示词？
→ `src/llm/llm_client.py`

### 想修改API接口？
→ `src/api/main.py`

### 想优化Agent对话？
→ `src/agent/job_agent.py`

### 想调整Neo4j导入逻辑？
→ `src/graph_builder/neo4j_importer.py`

---